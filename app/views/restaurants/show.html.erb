<h1>Show</h1>
<h3>Les avis de nos clients</h3>
<ul>
  <% @restaurant.reviews.each do |review| %>
    <li><%= review.content %> <%= review.rating %></li>
  <% end %>
</ul>
<h3>Déposer un nouvel avis</h3>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit %>
<% end %>




<%= link_to "Back to restaurants", root_path %>
